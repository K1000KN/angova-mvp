import React from "react";
import "./Loader.css";

const Loader = () => {
  return <div className="loader"></div>;
};

Loader.defaultProps = {
  size: "50px",
  color: "#fff",
};

export default Loader;
